NEXT_PUBLIC_API_BASE_URL=http://127.0.0.1:8000
# For npm run contract:verify against the real running API:
EMPOROS_OPENAPI_URL=http://127.0.0.1:8000/openapi.json

# In the REPOSITORY ROOT .env (read by the Python API), set:
# API_CORS_ORIGINS=http://127.0.0.1:3000,http://localhost:3000
# Restart the API after changing its CORS setting.
