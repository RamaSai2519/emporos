"""Robustness analyses over a finished run: does the result survive resampling, change and cost?

Everything here reads closed trades or reports; nothing touches the engine, so a check can never
change the run it judges (EM-114).
"""
